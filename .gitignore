# devenv
.devenv# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Byte-compiled / optimized / DLL Python files
__pycache__/
*.py[codz]
*$py.class

# macOS
.DS_Store
.Trash
result

# ai-gen docs
docs/

# per-machine build-time history (local, not synced to secrets)
.build-times

# crawler (rpi) image artifacts + host key — never commit these
*.img
*.img.zst
crawler-sd.img
crawler_host_ed25519
crawler_host_ed25519.pub
